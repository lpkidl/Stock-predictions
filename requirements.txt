# Core async and web frameworks
aiohttp>=3.9.0
httpx>=0.25.0

# Financial data
yfinance>=0.2.30
pandas>=2.0.0
numpy>=1.24.0

# Technical analysis
ta>=0.10.0

# Machine learning and NLP
xgboost>=2.0.0
torch>=2.0.0
transformers>=4.30.0
scikit-learn>=1.3.0

# UI and visualization
streamlit>=1.28.0
plotly>=5.17.0
altair>=5.0.0

# Configuration and security
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Logging and utilities
python-dateutil>=2.8.0
pytz>=2023.3.post1

# Scheduler
apscheduler>=3.10.0

# Optional social data sources
# praw: official Reddit API (replaces broken raw HTTP scraper)
praw>=7.7.1
# twikit: X/Twitter scraper (requires free X account credentials in .env)
twikit>=2.3.0
# pandas-ta: optional accelerator for TechnicalIndicatorEngine.
# All indicators (Ichimoku, ATR, ADX, BBW) are implemented in pure pandas/numpy
# and work without this package.  Install only if pandas-ta supports your Python
# version (requires Python <3.14 due to its numba dependency as of mid-2025):
#   pip install pandas-ta==0.4.71b0
# pandas-ta  # uncomment when using Python <=3.13
